# CODEOWNERS — @nick reviews everything.
#
# The handle must resolve on the host this repository lives on. This enterprise
# is GitHub Enterprise Cloud with data residency, whose accounts are
# EMU-provisioned through Google SAML and share no namespace with github.com,
# so a handle carried over from a github.com checkout resolves to nothing here.
#
# GitHub does not reject an owner it cannot resolve: it drops the rule and
# leaves those paths unowned, which is indistinguishable from having no
# CODEOWNERS at all. A repository can sit for months with the review gate on,
# the file committed, and no owner on any path. `navigator ops github setup`
# resolves every owner named here against the API for exactly that reason, and
# refuses to write the gate when one does not resolve.
#
# https://docs.github.com/articles/about-code-owners
* @nick
